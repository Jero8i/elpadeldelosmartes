{
  "name": "padel-pairs",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "esbuild main.ts --bundle --target=es2021 --minify --outfile=main.js",
    "watch": "esbuild main.ts --bundle --target=es2021 --outfile=main.js --watch",
    "typecheck": "tsc --noEmit",
    "serve": "python3 -m http.server 5173"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "typescript": "^5.3.3"
  }
}
